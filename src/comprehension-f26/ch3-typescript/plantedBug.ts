/**
 * COMPREHENSION EXERCISE 3 — Review the AI's tests.
 *
 * Open `plantedBug/average.ts`. It contains an implementation an AI assistant
 * wrote from a written specification, together with the four test cases it
 * wrote for itself. All four pass. The implementation is still wrong.
 *
 * Your job is not to fix the code. It is to produce the evidence -- to find one
 * input where the implementation disagrees with the specification it was given.
 *
 * Put that input below. The test will check three things:
 *   1. the assistant's own test cases really do all pass (its suite is green),
 *   2. your input is not simply one of the cases it already wrote,
 *   3. running the implementation on your input disagrees with the spec.
 *
 * This is the skill the rest of the AI work in this course depends on: an
 * assistant's tests describe the cases it thought of, and the bug is always in
 * the cases it did not.
 */
export const COUNTEREXAMPLE: number[] = [1, 2, 3];
