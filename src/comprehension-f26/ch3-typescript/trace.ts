/**
 * COMPREHENSION EXERCISE 1 — Trace the pipeline.
 *
 * Open `expressions.ts` and read e1 through e4. Work out by hand what each one
 * evaluates to, and write your answers below. Do not run the code first; the
 * whole point is to find out whether your model of `filter`, `map` and `reduce`
 * matches what they actually do.
 *
 * The test runs the real expressions and compares them to your predictions.
 *
 * Watch e4 in particular: `reduce` takes a starting value, and it is not always 0.
 */
export const PREDICTED_E1: number[] = [];
export const PREDICTED_E2: number[] = [];
export const PREDICTED_E3 = 0;
export const PREDICTED_E4 = 0;
